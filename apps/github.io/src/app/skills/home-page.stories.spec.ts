import meta from './home-page.stories';

describe('HomePage stories', () => {
  it('uses the Home Page Storybook hierarchy', () => {
    expect(meta.title).toBe('Components/Skills/Home Page');
  });
});
