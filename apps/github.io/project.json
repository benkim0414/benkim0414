{
  "name": "github.io",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/github.io/src",
  "projectType": "application",
  "tags": ["release:deployable"],
  "// targets": "to see all targets run: nx show project github.io --web",
  "targets": {
    "build": {
      "inputs": ["...", { "env": "APP_RELEASE" }, { "env": "APP_VERSION" }]
    },
    "verify-global-layout-css": {
      "dependsOn": ["build"],
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/github.io",
        "command": "node scripts/verify-global-layout-css.mjs"
      }
    },
    "verify-mobile-layout-browser": {
      "dependsOn": ["build"],
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/github.io",
        "command": "node scripts/verify-mobile-layout-browser.mjs"
      }
    }
  }
}
